package ru.otus.SpringIntegrationProject.utils;

import lombok.experimental.UtilityClass;

@UtilityClass
public class Utils {
//    public Person increasePayment(Person person, int amount) {
//        return Person.of(person.getName(), person.isNotManicured(), person.isWithoutMakeUp(), amount);
//    }

//    public Person changeMakeUpCondition(Person person, boolean withoutMakeUp, int amount) {
//        return Person.of(person.getName(), person.isNotManicured(), withoutMakeUp, person.getClientDebt() + amount);
//    }
//
//    public Person changeManicureCondition(Person person, boolean notManicured, int amount) {
//        return Person.of(person.getName(), notManicured, person.isWithoutMakeUp(), person.getClientDebt() + amount);
//    }
}
